Update this file with current project rules, preferences, and dependencies after the initial setup.
This file is meant to be a living document that evolves with the project, so feel free to update it
as needed.

## Rules

- Always use `uv` instead of `pip` for Python package management (e.g. `uv sync`, `uv add`).
